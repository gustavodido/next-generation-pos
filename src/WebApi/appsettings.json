{
  "ConnectionString": "User ID=next-generation-pos;Password=next-generation-pos;Host=localhost;Port=8093;Database=next-generation-pos;Pooling=true;",

  "GetProductsQuery": "SELECT uuid as Id, name as Name, ean_code AS EanCode, price as Price from products ORDER BY name",
  "SearchProductsQuery": "SELECT uuid as Id, name as Name, ean_code AS EanCode, price as Price from products WHERE lower(name) LIKE '%' || @Term || '%'  ORDER BY name",

  "Logging": {
    "IncludeScopes": false,
    "Debug": {
      "LogLevel": {
        "Default": "Warning"
      }
    },
    "Console": {
      "LogLevel": {
        "Default": "Warning"
      }
    }
  }
}
